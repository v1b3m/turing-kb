**Ref:** main
**Depends:** 2026-03-01_001_more-ui-updates

### Context

Clicking the **AND** button next to any of the filters in the knowledge filters UI should add a new filter row at the bottom like so

![[Pasted image 20260302003628.png]]

Choosing a field should reveal the **AND** and **OR** buttons for that filter too

![[Pasted image 20260302003842.png]]

If not value is selected, the **OR** button is disabled like in the screenshot above

Hitting enter will redirect to the view satisfying the filters like usual - reload -> show filtered view

![[Pasted image 20260302004009.png]]

For the above view, I input 12 and hit "Enter"

### Acceptance Criteria
- [ ] AND filter button behaves as expected

### Constraints
- None

### Resources
- None

---

### Handoff

| Field | Details |
|---|---|
| **What was done** | Made AND button functional on filter rows and toolbar. Clicking AND adds a new dynamic filter row with field/operator/value controls. When field is selected, AND/OR buttons appear; OR is disabled until a value is entered. Enter key or Run button applies filters to the table and updates the breadcrumb. |
| **Commit hash(es)** | `c874487` |
| **Files touched** | `src/components/knowledge/FilterPanel.tsx`, `src/components/knowledge/KnowledgeArticlesTable.tsx`, `src/stores/useKnowledgeStore.ts` |
| **Decisions made** | 1. Kept the 3 existing static/hardcoded filter rows as-is and added dynamic rows below them. 2. Used `articleBody` field for "Keywords" since that's the content field. 3. Default operator set to "are" when field is chosen (matches reference screenshot). 4. AND filters stored in Zustand store (`andFilters`) so they persist across filter panel open/close. 5. Breadcrumb shows `Field = value` format for AND filters. |
| **Known limitations** | 1. OR button on dynamic rows is rendered but not functional (that's the next task 2026-03-02_004). 2. Static rows' operators/values are not editable (they are display-only). 3. The "Save..." and "Add Sort" buttons in the toolbar remain cosmetic. |
| **How to verify** | 1. Open the Knowledge page. 2. Click the filter icon to open the filter panel. 3. Click AND (toolbar or per-row) → new empty row appears. 4. Select a field (e.g., Keywords) → AND/OR buttons appear, OR is disabled. 5. Type a value (e.g., "12") → OR becomes enabled. 6. Press Enter → table filters, breadcrumb shows "Keywords = 12". 7. Click X on a dynamic row to remove it. 8. `npm run build` succeeds. |
