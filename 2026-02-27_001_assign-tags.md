**Ref:** feat/knowledge-articles
**Depends:** none

### Context

Clicking "New tag" from the "Actions on selected rows" dropdown should first of all show a loading "modal" on top of the table and then show the "Tag Details" modal

**Loading:**

![[Screenshot from 2026-03-02 07-52-40.png]]

**Tag Details:**

![[Pasted image 20260227074619.png]]


### Acceptance Criteria
- [x] Clicking "New Tag" opens the loading modal followed by the "Tag Details" modal

### Constraints
- None

### Resources
- None

---

### Handoff

| Field | Details |
|---|---|
| **What was done** | Wired "New tag" button in ActionsDropdown to open a TagDetailsDialog. Dialog shows loading state (striped progress bar) for 1.5s then transitions to form with Name input and Viewable by toggle (Me / Groups and Users / Everyone) with Cancel/Save buttons. |
| **Commit hash(es)** | `dee6103` |
| **Files touched** | `src/components/knowledge/TagDetailsDialog.tsx` (new), `src/components/knowledge/KnowledgeArticlesHeader.tsx`, `src/app/globals.css`, `_SCRATCH.md` |
| **Decisions made** | Used 1.5s simulated loading time. Used Radix Dialog with positionBelowTop to match existing dialog patterns. Green border (#2e8540) on focused input to match ServiceNow style. Toggle button group for "Viewable by" using dark teal (#00566e) for selected state. |
| **Known limitations** | Save doesn't persist tag to store (no tag management store exists yet). Loading is simulated with a timeout, not tied to real API. |
| **How to verify** | Select rows in Knowledge table → click "Actions on selected rows" → click "New tag" → loading modal appears with striped bar → after 1.5s form appears → fill name, pick viewable by, click Save or Cancel. |
