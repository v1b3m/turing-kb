We have a situation:

We introduced multiple logins, however, I don't think we're clearing all items that should be cleared for the other users. Data from the original logged in user (Maya) is left over which should be cleared.

The diff should have shown removals of Maya's old income forms, deductions, credits, documents, linked accounts, and prior filed-return metadata. Instead, almost all of Maya's 2024/2025 tax footprint was carried over. This is the main trainer-execution issue.

Dates of births are also not consistent in multiple places:

`rm_tax_return.personalInfo.dateOfBirth` is correctly `1982-06-22`, but `auth.currentUser.dateOfBirth` and `users.state.users[Marcus].dateOfBirth` remain the seed value `1986-04-17`. The diff shows the requested DOB was applied only inside the tax-return slice, not to the account/profile.

I believe when we log in a new user who's undergoing onboarding, then all this other info should also be removed.

> [!important]
> The logic is if someone is undergoing onboarding, then the data should match that status (not onboarded)

