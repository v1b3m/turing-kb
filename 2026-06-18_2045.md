We have a bug in final review

If I fix a bug then that should persist — I should not have to fix that bug a second time

1. I'm on `/index/tto/final-review/fix-hub` page, I fix the bugs, all of them
2. I return to `index/tto/final-review` and go through the flow again, on returning to fix-hub, I see the same three errors again, this should not happen as I already fixed these issues
3. 