[R1] - 06/14/2026 - Tushar Chauhan

**Prompt**

```
The return is ready to go out. On the Ready to File page, check both the Federal and State (California) return checkboxes. Move to the Verify page and enter the prior year AGI of $96,720.52 and set a new 5-digit signing PIN. Then on the Submit page click 'Transmit returns now'. Once the Confirmation page loads, note the federal submission ID and check that a California refund summary card is showing, then navigate to the Status page to confirm the timeline has advanced past 'Submitted'.
```

**Issue 1:**

There is an inconsistency between the newly added return and the existing one.

```json
{
    "id": "3520da34-a1c1-5e8e-8ddb-09e5b7310f38",
    "taxYear": 2025,
    "filedAt": 1781433846389,
    "status": "pending",
    "refundOrBalance": 10966,
    "agi": 90240,
    "taxableIncome": 64364,
    "filingStatus": "Head of household",
    "taxpayerName": "Maya Srinivasan"
  },
  {
    "id": "eeff9127-82cf-5d4e-a1f0-96be4408b2e6",
    "taxYear": 2024,
    "filedAt": 1742205600000,
    "status": "accepted",
    "refundOrBalance": 3536.17,
    "federalPdfUrl": "/returnmax/sample-2024-return.pdf",
    "stateAbbr": "CA",
    "agi": 96720.52,
    "taxableIncome": 74280.1,
    "filingStatus": "head_of_household",
    "taxpayerName": "Maya Srinivasan"
  }
```

The first return does not include the `federalPdfUrl` and `stateAbbr` fields.

**Issue 2:**

I selected the federal tax payment method as `irs_credit_debit_card`, which is correct. However, I did not select any `paper_check` method.

Despite that, `stateTaxPayment`: `paper_check` was added.

These two fields were added.


**Issue 3:** 

location efile > 

A new field, `lastYearAGIUsd`, was added. I think this field should have already been included in the initial JSON since it is a predefined field and exists in the UI.

