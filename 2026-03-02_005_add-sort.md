**Ref:** main
**Depends:** 2026-03-01_001_more-ui-updates
### Context

![[Pasted image 20260302005044.png]]

Clicking "Add sort" in the filtered table view adds a sort row below the filters separated by a horizontal divider like shown above
### Acceptance Criteria
- [ ] clicking "Add Sort" adds a sort section

### Constraints
- None

### Resources
- None

---

### Handoff

| Field | Details |
|---|---|
| **What was done** | Wired the "Add Sort" button in FilterPanel to add a sort section below filters, with field dropdown, direction dropdown (a to z / z to a), and X remove button, separated by a horizontal divider with "Order results by the following fields" heading |
| **Commit hash(es)** | 9fb3ab7 |
| **Files touched** | `src/components/knowledge/FilterPanel.tsx` |
| **Decisions made** | Sort state is local to FilterPanel (not in store) since the task is UI-only. Default sort field is "Active" matching the reference screenshot. "Active" added as extra option since it's not in FILTER_FIELDS. |
| **Known limitations** | Sort rows are UI-only — they don't actually re-order the table data. Table has its own column-level sort via click headers. |
| **How to verify** | Open the filter panel, click "Add Sort" — sort section appears with divider. Change field/direction dropdowns. Click X to remove. Multiple sort rows can be added. `npm run build` passes. |
