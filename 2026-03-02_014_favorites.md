**Ref:** feat/knowledge-articles
**Depends:** none
### Context
I see another favorites button has been added to the toolbar. The favorites button MUST remain in the header. Yes, we may need to make some changes to the header to accommodate this but we cannot add a second favorites in the tool bar.
### Acceptance Criteria
- [x] Favorites button before the search input in the tool bar must go
- [x] All functionality transferred to the favorites button in the header
### Constraints
- None

### Resources
- None

---

### Handoff
| Field                 | Details                                                                                                                                                                                                  |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **What was done**     | Removed the favorites star button (`titleExtra`) from the Knowledge toolbar in `KnowledgeArticlesHeader.tsx`. Made the existing star in the NowService header pill interactive — it now shows green fill when favorited and opens `RemoveFavoriteDialog` on click. |
| **Commit hash(es)**   | `31c2f54`                                                                                                                                                                                                |
| **Files touched**     | `src/components/layout/Header.tsx`, `src/components/knowledge/KnowledgeArticlesHeader.tsx`, `_SCRATCH.md`                                                                                                |
| **Decisions made**    | Kept `CreateFavoriteDialog` in the hamburger menu (unchanged). Only moved the star toggle + `RemoveFavoriteDialog` to the header. Used `anchorRef` positioning so the dialog appears below the pill star. |
| **Known limitations** | None                                                                                                                                                                                                     |
| **How to verify**     | 1. Open the app — the toolbar should no longer have a star next to "Knowledge". 2. Use hamburger menu → Create Favorite — the NowService pill star should turn green. 3. Click the green star → RemoveFavoriteDialog appears anchored to the pill. |
