---
task_id: returnmax-medium-015
version: v1
prompt_uuid: 4b3b42b2-7ba1-4729-ae58-071877eaad2c
task_url: https://staging-rl-gym-harness.turing.com/verification/prompts/4b3b42b2-7ba1-4729-ae58-071877eaad2c/task
batch: ReturnMax Test Batch
status: verified
owner: claude-runner
claimed_at: 2026-06-15 11:18
updated_at: 2026-06-15 11:40
bug_count: 2
fix_pr: https://github.com/turing-rlgym/returnmax/pull/261
l1: Onboarding & Product Selection
l2: Return Setup & Resume
l3: Select Tax Year, Resume or Start a Return, and Review Tax Law Updates
persona: Returning User
complexity: Medium
ecosystem: Tax
est_hops: "3"
est_steps: "36"
est_actions: "3"
gym: Returnmax V1
gym_url: https://returnmax-v1.rlgym.turing.com
tags:
  - returnmax-test
  - rlgym
---

# returnmax-medium-015 (v1)

Batch index: [[2026-06-14_1939_returnmax-test-batch-prompts]] · [Open task page](https://staging-rl-gym-harness.turing.com/verification/prompts/4b3b42b2-7ba1-4729-ae58-071877eaad2c/task) · Gym: [Returnmax V1](https://returnmax-v1.rlgym.turing.com)

> [!warning] Before testing: claim + reset to clean state
> 1. Claim: set `owner`, `status: in-progress`, `claimed_at` (only if `owner` empty/stale). See [[_AGENT-PROTOCOL]].
> 2. Clean state: open `https://returnmax-v1.rlgym.turing.com/state-diff` → **Reset All** → confirm all sections **Clean**.
> 3. Drive the app through the prompt, then re-open **state-diff** to verify the change (oracle).

- Taxonomy: Onboarding & Product Selection › Return Setup & Resume › Select Tax Year, Resume or Start a Return, and Review Tax Law Updates
- Persona: Returning User | Complexity: Medium | Est. hops/steps/actions: 3/36/3

## Prompt

> Coming back to finish a return that was started last tax season. On the Your Tax Returns dashboard (/myreturnmax), select tax year 2025 and click Continue filing to get back to the Tax Home dashboard. From the Explore section, open the Tax Law Updates page and review the Standard Deduction and Child Tax Credit update entries, note the directional indicators for both. Then close Tax Law Updates and use the Continue filing button on the Tax Home dashboard to resume in the current section, which should be 'review'.

## Prep work

> Tax return for Maya Srinivasan (tax_return_id: 6aafcfc5) exists for tax year 2025 with status 'In progress' and current_section 'review'. The Your Tax Returns dashboard (/myreturnmax) shows year-selector buttons (2025, 2024, 2023) and return cards with per-return actions. The Tax Home dashboard's Explore section links to the Tax Law Updates page (/prior-year/tax-law-updates), which lists 6 categorized updates including Standard Deduction and Child Tax Credit with directional indicators (up/down/same). The Continue filing button on the Tax Home dashboard routes to the current_page '/index/tto/review'.

## Justification

A returning filer resumes an in-progress 2025 return from the dashboard, reviews the tax law changes relevant to their standard deduction and child tax credit before diving back in, then continues filing from the last saved section.

## Findings

**Run 2026-06-15 11:18–11:40 (claude-runner) — `localhost:3001`, baseline `ots_dev` @ `b73bdc6`. Latest version re-fetched from harness task page = v1, text matches note.**

1. **`/myreturnmax` — select tax year 2025 + "Continue filing".** ✅ works. Year selector (2025/2024/2023) present; the 2025 card is "In progress" with a **"Continue filing"** link → **`/index/tto`** (`app/myreturnmax/_client.tsx:181`). Note: on localhost this routes to the **legacy stepper home** (`/index/tto`, no Explore section), whereas the prompt's "Tax Home dashboard" (Explore + Continue filing) is the **unified** home at `/unified/2025/index` (the deployed gym's shell). Minor legacy/unified routing inconsistency; not the focus.
2. **Tax Home Explore → Tax Law Updates.** ⚠️ **BUG #1 (fixed).** On the unified Tax Home (`/unified/2025/index`), the Explore card **"Catch up on the new tax laws"** called `showComingSoon(...)` (`app/unified/[year]/index/_client.tsx:157`) — a "Coming soon" stub — even though the **Tax Law Updates page exists and works** at `/index/tto/prior-year/tax-law-updates`. Verified the page directly: **Standard Deduction = "increased for 2025"** (▲) and **Child Tax Credit = "remains $2,000 per child"** (■ same) — both directional indicators render. The page was simply unreachable from the dashboard. **Fixed** → card now `router.push("/index/tto/prior-year/tax-law-updates")` (PR #261); re-verified live: click lands on the page, no modal.
3. **Tax Home "Continue filing" → resume at saved section 'review'.** ⚠️ **BUG #2 (deferred).** The unified "Continue filing" (`handleContinue`, `app/unified/[year]/index/_client.tsx:131`) **hardcodes** `setSectionStatus("personal_info", …)` + `router.push("${base}/tto")` and the "Up next" card is titled "My personal info" — it **ignores** the saved `rm_tax_ui.currentSection` (= **"review"**) / `currentPage` (= **"/index/tto/review"**). Confirmed live: Continue filing lands on `/unified/2025/index/tto` (personal info), **not** the review section. So step 3 ("resume in the current section, which should be 'review'") fails. (The legacy `/index/tto` home has the same defect: `resumeSection = null` → defaults to `personal_info`, `_client.tsx:162`.)

State-diff oracle: navigation-only prompt; `setSectionStatus` writes `progressBySection.personal_info`, no return-data mutation. State restored to seed.

**Verdict: bug-found → fix-pushed (1 of 2 fixed).** BUG #1 (dead Explore link) fixed surgically in PR #261. BUG #2 (Continue filing ignores the saved section) deferred — a correct "resume where you left off" needs (a) reading `currentSection`/`currentPage` from `rm_tax_ui` and (b) a section→route map, complicated by the seed's `currentPage: "/index/tto/review"` being a **non-existent route** (only `expert-review`/`federal-review`/`final-review` exist; the `review` section maps to `/index/tto/expert-review` in the stepper). That ambiguity (which "review" route?) is a product decision, so not patched blind.

## Bugs found

_One bullet per bug; keep `bug_count` in sync._

- **[FIXED — PR #261] Tax Home Explore card "Catch up on the new tax laws" was a dead "Coming soon" stub despite the Tax Law Updates page existing.** `app/unified/[year]/index/_client.tsx:157` called `showComingSoon("Catch up on the new tax laws")` instead of navigating to `/index/tto/prior-year/tax-law-updates` (a fully-populated page: Standard Deduction ▲, Child Tax Credit ■, etc.). Fixed by routing the card to that page (matching the existing `/index/tto/final-review` link pattern). Re-verified live.
- **[DEFERRED] "Continue filing" ignores the saved section and always resumes at personal info.** Both the unified home (`handleContinue` hardcodes `personal_info` + pushes `${base}/tto`, `app/unified/[year]/index/_client.tsx:131`) and the legacy home (`resumeSection = null`, `app/index/tto/_client.tsx:162`) ignore `rm_tax_ui.currentSection`/`currentPage` (seed = `"review"` / `"/index/tto/review"`). The prompt expects resume at the 'review' section; instead it lands on personal info. **Fix deferred** — needs a section→route resume map and resolution of the seed's dead `currentPage: "/index/tto/review"` route (product decision on which review page 'review' resolves to).

## Fix

_Link PR/commit once pushed; set status fix-pushed → verified._

- PR #261 (BUG #1): https://github.com/turing-rlgym/returnmax/pull/261 — Explore "new tax laws" card now opens `/index/tto/prior-year/tax-law-updates`. Verified live (lands on page, indicators visible, no "Coming soon").
- BUG #2: _deferred_ — resume-at-saved-section; see Bugs found.

