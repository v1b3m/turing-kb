---
task_id: "returnmax-medium-034"
version: "v1"
prompt_uuid: "f7e55a11-e8dd-413f-bd9c-ecca2cf04b77"
task_url: "https://staging-rl-gym-harness.turing.com/verification/prompts/f7e55a11-e8dd-413f-bd9c-ecca2cf04b77/task"
batch: "ReturnMax Test Batch"
# --- coordination (source of truth for multi-agent runs) ---
status: bug-found        # untested | in-progress | passed | bug-found | fix-pushed | verified
owner: claude-runner                  # agent id holding the claim; empty = available
claimed_at: 2026-06-15 14:54             # ISO-8601 UTC+3 when claimed (stale lease if old)
updated_at: 2026-06-15 15:00             # ISO-8601 UTC+3 of last status change
# --- results ---
bug_count: 1
fix_pr:                 # PR/commit URL once a fix is pushed
# --- prompt metadata (immutable, latest version) ---
l1: "Onboarding & Product Selection"
l2: "Return Setup & Resume"
l3: "Complete Guided Onboarding with Situation Check"
persona: "First-Time Filer"
complexity: "Medium"
ecosystem: "Tax"
est_hops: "3"
est_steps: "45"
est_actions: "4"
gym: "Returnmax V1"
gym_url: "https://returnmax-v1.rlgym.turing.com"
tags: [returnmax-test, rlgym]
---

# returnmax-medium-034 (v1)

Batch index: [[2026-06-14_1939_returnmax-test-batch-prompts]] · [Open task page](https://staging-rl-gym-harness.turing.com/verification/prompts/f7e55a11-e8dd-413f-bd9c-ecca2cf04b77/task) · Gym: [Returnmax V1](https://returnmax-v1.rlgym.turing.com)

> [!warning] Before testing: claim + reset to clean state
> 1. Claim: set `owner`, `status: in-progress`, `claimed_at` (only if `owner` empty/stale). See [[_AGENT-PROTOCOL]].
> 2. Clean state: open `https://returnmax-v1.rlgym.turing.com/state-diff` → **Reset All** → confirm all sections **Clean**.
> 3. Drive the app through the prompt, then re-open **state-diff** to verify the change (oracle).

- Taxonomy: Onboarding & Product Selection › Return Setup & Resume › Complete Guided Onboarding with Situation Check
- Persona: First-Time Filer | Complexity: Medium | Est. hops/steps/actions: 3/45/4

## Prompt

> First time through the TTO onboarding flow and it needs to be completed cleanly. From the welcome screen on the UnifiedOnboardingPage, acknowledge the welcome and advance to loading. On the situations form, check 'Has W-2 job', 'Has children or dependents', 'Owns home', and 'Sold stock or crypto', confirm no conflict validation fires. Leave 'Is self-employed' unchecked. Advance to the verify step and enter date of birth '03/14/1988' and SSN last four '9876' using the SSN method. Complete the moreInfo step by entering country 'United States' and address query '2148 Valencia Street, San Francisco, CA 94110'. On the priorYearFiling step, select method 'returnmax'. Confirm the Tax Home dashboard loads showing the 'Up next' card.

## Prep work

> User Maya Srinivasan (user_id: aecd2ba0) has has_onboarded: true but the onboarding flow is accessible for review. The UnifiedOnboardingPage phases are: welcome, loading, situations, verify, moreInfo, priorYearFiling. Situations checkboxes include: hasW2Job, isFreelancerGigWorker, isSelfEmployed, ownsSCorpPartnershipLlc, hasUnemploymentIncome, hasChildrenDependents, ownsHome, paidRent, ownsRentalProperty, soldStockOrCrypto, donatedToCharity, maximizeDeductionsCredits. Valid situations to select: hasW2Job, hasChildrenDependents, ownsHome, soldStockOrCrypto. Conflict validation fires for incompatible combinations. Verify step accepts dateOfBirth '1988-03-14' and ssnLastFour '9876' via SSN method. moreInfo address: '2148 Valencia Street, San Francisco, CA 94110'. priorYearFiling method: 'returnmax'.

## Justification

A first-time user completes all five phases of the UnifiedOnboardingPage, welcome, situations, verify, moreInfo, and priorYearFiling, entering accurate employment, household, and prior year filing details, then confirms the Tax Home dashboard's 'Up next' card is the entry point for the return.

## Findings

**Run 2026-06-15 14:54–15:00 (claude-runner) — `localhost:3001`, baseline `ots_dev` @ `42a0d1b`. Clean seed.**

The onboarding UI flow is **unreachable for the seed user**, so the prompt's welcome→…→priorYearFiling sequence cannot be driven.

1. **`/onboarding` redirects onboarded users to Tax Home.** `app/onboarding/_client.tsx:17-20` — `if (currentUser.hasOnboarded) router.replace(UNIFIED_TAX_HOME_PATH)`. Live: navigating to `/onboarding` immediately lands on **`/unified/2025/index`** (Tax Home). The seed user has `hasOnboarded: true`, so the onboarding page never renders. This directly contradicts the prep's claim "has_onboarded: true **but the onboarding flow is accessible for review**."
2. **Seed already at the onboarding end-state.** The seed's `onboardingInfo` has every phase flag **true** (`hasAcknowledgedWelcome/hasCompletedSituations/hasCompletedVerification/hasCompletedMoreInfo`) and all target data already filled: `situations {hasW2Job, hasChildrenDependents, ownsHome, soldStockOrCrypto = true; isSelfEmployed = false}`, `verification {dateOfBirth:"1988-03-14", ssnLastFour:"9876", method:"ssn"}`, `moreInfo {country:"United States", addressQuery:"2148 Valencia Street, San Francisco, CA 94110"}`, `priorYearFiling {method:"returnmax"}` — exactly the prompt's values. So even if the redirect were removed, `initialPhase()` (`UnifiedOnboardingPage.tsx:31-42`) computes from these flags and would resolve to **`priorYearFiling`**, never `welcome`. There is no "review from scratch" mode that resets to the welcome screen.
3. **Step 6 — Tax Home 'Up next' card.** ✅ reachable directly: `/unified/2025/index` renders the **"Up next"** card ("Last saved on Jun 15 · My personal info · Continue filing"). The prompt's final destination exists.

**Consequence:** the entire prompt flow (acknowledge welcome → loading → check the 4 situations w/ no conflict → verify DOB+SSN → moreInfo address → priorYearFiling 'returnmax') **cannot be performed via the UI** — onboarding redirects away, and the data is already pre-completed to the target values. Only the end-state (Tax Home 'Up next') is observable.

State-diff oracle: no mutation possible (redirected before any onboarding interaction). The seed already equals the prompt's intended end-state.

**Verdict: bug-found (seed/prompt mismatch, fix deferred).** The prompt is a *first-time* onboarding scenario, but the seed user is **fully onboarded** with all `onboardingInfo` pre-filled, and the app correctly redirects onboarded users out of `/onboarding`. Making the flow performable needs a **seed change** (an un-onboarded user + empty `onboardingInfo`), which is gym-data scope — not an app code fix. The redirect guard is arguably-correct product behavior. Same disposition class as medium-024/026 (seed/flow mismatch).

## Bugs found

_One bullet per bug; keep `bug_count` in sync._

- **The first-time onboarding flow is unreachable for the seed user: `/onboarding` redirects `hasOnboarded:true` users to Tax Home, and the seed's `onboardingInfo` is already fully completed to the prompt's exact target values.** `app/onboarding/_client.tsx:17-20` redirects onboarded users to `/unified/2025/index` (verified live). Even absent the redirect, `initialPhase()` (`UnifiedOnboardingPage.tsx:31-42`) would start at `priorYearFiling` (all phase flags true in seed), not `welcome`. So none of the prompt's UI steps (welcome → situations → verify → moreInfo → priorYearFiling) can be driven; only the Tax Home 'Up next' card (end state) is reachable, and the target data already matches the seed. **Fix deferred** — this is a seed/prompt mismatch (needs an un-onboarded seed user with empty `onboardingInfo`), gym-data scope rather than a surgical app patch; the redirect itself is reasonable product behavior.

## Fix

_Link PR/commit once pushed; set status fix-pushed → verified._

- PR: 

