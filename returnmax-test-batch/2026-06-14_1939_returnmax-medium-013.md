---
task_id: returnmax-medium-013
version: v1
prompt_uuid: 8c43f17b-0b13-45ba-9756-655e08508694
task_url: https://staging-rl-gym-harness.turing.com/verification/prompts/8c43f17b-0b13-45ba-9756-655e08508694/task
batch: ReturnMax Test Batch
status: verified
owner: claude-runner
claimed_at: 2026-06-15 10:55
updated_at: 2026-06-15 11:32
bug_count: 1
fix_pr: https://github.com/turing-rlgym/returnmax/pull/267
l1: Expert Assistance
l2: Live Expert Help
l3: Start a Live Chat or Screen Share Session with a Tax Expert
persona: Expert-Assisted Filer
complexity: Medium
ecosystem: Tax
est_hops: "3"
est_steps: "38"
est_actions: "3"
gym: Returnmax V1
gym_url: https://returnmax-v1.rlgym.turing.com
tags:
  - returnmax-test
  - rlgym
---

# returnmax-medium-013 (v1)

Batch index: [[2026-06-14_1939_returnmax-test-batch-prompts]] · [Open task page](https://staging-rl-gym-harness.turing.com/verification/prompts/8c43f17b-0b13-45ba-9756-655e08508694/task) · Gym: [Returnmax V1](https://returnmax-v1.rlgym.turing.com)

> [!warning] Before testing: claim + reset to clean state
> 1. Claim: set `owner`, `status: in-progress`, `claimed_at` (only if `owner` empty/stale). See [[_AGENT-PROTOCOL]].
> 2. Clean state: open `https://returnmax-v1.rlgym.turing.com/state-diff` → **Reset All** → confirm all sections **Clean**.
> 3. Drive the app through the prompt, then re-open **state-diff** to verify the change (oracle).

- Taxonomy: Expert Assistance › Live Expert Help › Start a Live Chat or Screen Share Session with a Tax Expert
- Persona: Expert-Assisted Filer | Complexity: Medium | Est. hops/steps/actions: 3/38/3

## Prompt

> Running into a question about whether the VTI ETF gain changes my bracket before submitting. Open the Expert Help FAB and start a Live Chat session. Once connected to a tax expert, ask whether the $550.54 long-term capital gain from Fidelity Investments affects the effective tax rate at the current income level. Then close that chat and open a Screen Share session instead, entering the session code when prompted so the expert can see the capital gains entry on the 1099-B page.

## Prep work

> Tax return for Maya Srinivasan (tax_return_id: 6aafcfc5) is in progress; expert_help_open is currently false. The Expert Help FAB (96 px, bottom-right, always visible) opens expert assistance options. The Live Chat page (/expert/chat) connects to a tax expert with a chat interface including message display, text input, file attachment, and end-chat button. The Screen Share page (/expert/screenshare) initiates a cobrowse session with a session code input and three security benefit cards. Capital gains from Fidelity Investments exist: VTI ETF long-term gain $550.54 (income_item_id: cdb16bd4). Current effective tax rate is 5.1% and marginal bracket is 22%.

## Justification

A filer with a capital gains question uses Expert Help first through live chat to get a quick answer, then escalates to a screen share so the expert can review the specific 1099-B entry, testing both expert contact methods in a single session.

## Findings

**Run 2026-06-15 10:55–11:02 (claude-runner) — `localhost:3001`, baseline `ots_dev` @ `b73bdc6`. Latest version re-fetched from harness task page = v1, text matches note.**

The prompt requires a working **live chat** and **screen share** with an expert. Both are
non-functional mockups — every action routes to a "Coming soon" modal (`useComingSoonAction`,
`lib/utils.showComingSoon`). Verified against source + live app:

1. **"Open the Expert Help FAB and start a Live Chat session."** ✗ The Expert Help panel
   (`components/app/ExpertHelpPanel.tsx`) opens, but its **"Connect with an expert"** CTA calls
   `showComingSoon("Connect with an expert")` (line 177) — clicking it shows **"Coming soon"**
   (confirmed live). There is **no UI link anywhere to `/expert/chat`** (grep for `expert/chat`
   across `app/` + `components/` finds zero navigations — the route is orphan, reachable only by URL).
2. **"ask whether the $550.54 LTCG affects the effective tax rate."** ✗ Even via direct URL,
   `/expert/chat` (`app/index/tto/expert/chat/_client.tsx`) is static: `messages = useState(initialMessages)`
   with no setter (only Sarah's canned greeting), and the **Send button calls `showComingSoon("Send message")`**
   — you cannot post a message or get an answer.
3. **"close that chat."** ✗ The **End chat** button calls `showComingSoon("End chat")` — it never closes.
4. **"open a Screen Share session, entering the session code when prompted."** ✗ No UI link to
   `/expert/screenshare` either (orphan route). The page (`.../screenshare/_client.tsx`) has a session-code
   `input` (always visible, never "prompted"), but **"Start screen share"** → `showComingSoon("Screen share")`
   and **"Connect"** → `showComingSoon("Connect with session code")`. No session ever starts.

State-diff oracle: no app state mutates (coming-soon modals don't persist; `expert_help_open` is the
only related flag and the live-chat/screenshare features write nothing). State restored to seed.

**Verdict: bug-found (feature stub / app gap, fix deferred).** The Live Expert Help feature the prompt
targets (live chat + screen share) is deliberately unimplemented — surfaced as "Coming soon" — and the
`/expert/chat` & `/expert/screenshare` pages are orphan mockups nothing links to. Same class as
medium-010 (EIC stub) and medium-011 (settings model): the app intentionally hasn't built this, so the
prompt is unachievable. No surgical fix — building functional expert chat + cobrowse is feature-scope work.

## Bugs found

_One bullet per bug; keep `bug_count` in sync._

- **Live Expert Help (chat + screen share) is non-functional — every action is a "Coming soon" stub, and the pages are orphan routes.** `ExpertHelpPanel`'s "Connect with an expert" → `showComingSoon` (`components/app/ExpertHelpPanel.tsx:177`); `/expert/chat` Send/End/Attach all → `showComingSoon` (`app/index/tto/expert/chat/_client.tsx`), messages are a static `useState` with no setter; `/expert/screenshare` Start/Connect → `showComingSoon` (`.../screenshare/_client.tsx`). Nothing in `app/`+`components/` navigates to `/expert/chat` or `/expert/screenshare` (orphan, URL-only). Consequence: a filer cannot start a live chat, ask a question, end a chat, or start a screen share — the prompt's entire flow is impossible. **Fix deferred** — implementing real expert chat + cobrowse is a feature build, not a surgical patch (same disposition as medium-010 / medium-011).

## Fix

_Link PR/commit once pushed; set status fix-pushed → verified._

- PR: _deferred_ — see Bugs found. Expert live chat / screen share are intentional "Coming soon" stubs; building them is feature-scope work needing product input. Flagged, not implemented.

