---
task_id: "returnmax-medium-014"
version: "v1"
prompt_uuid: "427d5181-74a5-4405-9b7d-19440ebf26f8"
task_url: "https://staging-rl-gym-harness.turing.com/verification/prompts/427d5181-74a5-4405-9b7d-19440ebf26f8/task"
batch: "ReturnMax Test Batch"
# --- coordination (source of truth for multi-agent runs) ---
status: fix-pushed      # untested | in-progress | passed | bug-found | fix-pushed | verified
owner: claude-runner    # agent id holding the claim; empty = available
claimed_at: 2026-06-15 11:05  # ISO-8601 UTC+3 when claimed (stale lease if old)
updated_at: 2026-06-15 11:57  # ISO-8601 UTC+3 of last status change
# --- results ---
bug_count: 1
fix_pr: "https://github.com/turing-rlgym/returnmax/pull/271"  # PR/commit URL once a fix is pushed
# --- prompt metadata (immutable, latest version) ---
l1: "State Tax Filing"
l2: "State Return Preparation"
l3: "Prepare State Return with Auto-Filled Federal Data"
persona: "First-Time Filer"
complexity: "Medium"
ecosystem: "Tax"
est_hops: "4"
est_steps: "45"
est_actions: "4"
gym: "Returnmax V1"
gym_url: "https://returnmax-v1.rlgym.turing.com"
tags: [returnmax-test, rlgym]
---

# returnmax-medium-014 (v1)

Batch index: [[2026-06-14_1939_returnmax-test-batch-prompts]] · [Open task page](https://staging-rl-gym-harness.turing.com/verification/prompts/427d5181-74a5-4405-9b7d-19440ebf26f8/task) · Gym: [Returnmax V1](https://returnmax-v1.rlgym.turing.com)

> [!warning] Before testing: claim + reset to clean state
> 1. Claim: set `owner`, `status: in-progress`, `claimed_at` (only if `owner` empty/stale). See [[_AGENT-PROTOCOL]].
> 2. Clean state: open `https://returnmax-v1.rlgym.turing.com/state-diff` → **Reset All** → confirm all sections **Clean**.
> 3. Drive the app through the prompt, then re-open **state-diff** to verify the change (oracle).

- Taxonomy: State Tax Filing › State Return Preparation › Prepare State Return with Auto-Filled Federal Data
- Persona: First-Time Filer | Complexity: Medium | Est. hops/steps/actions: 4/45/4

## Prompt

> The California return pulled in the federal data automatically but a few adjustments are still needed. On the State Adjustments page, enter U.S. bond interest as a subtraction of $0 (confirming nothing applies) and add the Social Security exemption subtraction for the correct amount. Then on the State Deductions and Credits page, enter the state disability insurance amount from Box 14 of the W-2 (CASDI: $720.00) as an additional state deduction. Review the state refund amount in the header pill and confirm it shows the California refund before clicking 'Continue to Final Review'.

## Prep work

> Tax return for Maya Srinivasan (tax_return_id: 6aafcfc5) is in progress; California (CA) state return (state_return_id: 02b3ccc5) exists with status 'completed' and return type 'resident'. The State intro page (/index/tto/state) explains federal data has transferred. State Adjustments page exists with subtraction options including U.S. bond interest and Social Security exemption. State Deductions and Credits page (/state/credits) exists; CASDI amount from W-2 Box 14 is $720.00 and is a valid state deduction entry. The state refund amount for California is $815.50 and should be visible in the header pill. The 'Continue to Final Review' button appears after reviewing state sections.

## Justification

A California resident filer confirms that federal data auto-transferred to the state return, enters a CASDI deduction from their W-2 Box 14, reviews the state adjustments page for accuracy, and verifies the California refund estimate before advancing to Final Review.

## Findings

**Run 2026-06-15 11:05–11:14 (claude-runner) — `localhost:3001`, baseline `ots_dev` @ `b73bdc6`. Latest version re-fetched from harness task page = v1, text matches note.**

Steps 1–2 are achievable; step 3 (state-refund header pill + "Continue to Final Review") is **not**.

1. **State Adjustments — U.S. bond interest subtraction $0 + Social Security exemption "correct amount".** ✅ achievable. `/index/tto/state/adjustments` (`_client.tsx`) has the subtraction fields **"U.S. government bond interest"** (`govBondInterest`) and **"Social Security benefits (if state-exempt)"** (`socialSecurityExempt`). Set both to `0` (live), persisted to `paymentInfo.stateAdjustments`. The **"correct amount" for the SS subtraction is $0** — the seed has **no Social Security benefits** (no `ssa1099`/SS income), so there's nothing for CA to exempt. (CA exempts SS entirely, but Maya has none.)
2. **State Deductions & Credits — CASDI $720.** ✅ field exists. `/index/tto/state/credits` (`_client.tsx`) "State disability insurance (SDI)" → `stateDisabilityInsurance`. **Stale seed:** the value is **already `"720"`** (`paymentInfo.stateDeductionsCredits.stateDisabilityInsurance = "720"`, plus `rentersCredit "120"`), so nothing to add. Field is editable. Same stale-seed pattern as 001/002/004/010.
3. **Review state refund "in the header pill" = $815.50, then "Continue to Final Review".** ✗ **not achievable with the seed.**
   - **No state-refund header pill exists.** On all state pages the header shows only **"Federal refund"** (confirmed live). The CA refund is `stateReturns[0].stateRefundOrOwed = 815.5`, but it is rendered only in the **body** of `/state/review` (Branch B) and on `/final-review` — never in the header pill the prompt names.
   - **The $815.50 pill and the "Continue to Final Review" button are gated behind `!hasCompletedStates`.** `/index/tto/state/review/_client.tsx:18` branches: when any state has `status === "completed"` (the seed: CA **completed**), it renders the **"We found 1 item on your CA return … Fix Return"** screen (lines 19–73) — which has **no refund pill and no Continue-to-Final-Review** (only "Choose a State" / "Back" / "Fix Return" → `/other`). The refund pill ($815.50, line 81–93) and **"Continue to Final Review"** (line 162–168 → `/final-review`) live only in the `else` branch. Confirmed live: `/state/review` shows "Let's make sure everything is accurate … Fix Return", `has815=false`, `hasContinueFinal=false`.

State-diff oracle: entering the adjustments writes only `paymentInfo.stateAdjustments.{govBondInterest,socialSecurityExempt}`; no refund recomputation (these fields feed no state-tax engine — `stateRefundOrOwed` is a stored seed value, not derived). State restored to seed.

**Verdict: bug-found (app/logic gap on step 3, fix deferred).** A **"completed"** state return dead-ends on `/state/review` to "Fix Return" and can never reach "Continue to Final Review" from that page, and the app has **no state-refund header pill** for the prompt to read. The prompt+prep_work are also internally inconsistent (they assert state status `completed` *and* expect the refund pill + Continue-to-Final-Review, which the app's branch makes mutually exclusive). Steps 1–2 work.

## Bugs found

_One bullet per bug; keep `bug_count` in sync._

- **A "completed" state return cannot reach "Continue to Final Review" from `/state/review`, and no state-refund pill is shown in the header.** `app/index/tto/state/review/_client.tsx:18` gates the state-refund display ($815.50, lines 81–93) and the "Continue to Final Review" button (lines 162–168 → `/final-review`) behind `!hasCompletedStates`; when a state is `status:"completed"` (the seed's CA return) the page instead shows "We found 1 item … Fix Return" (→ `/other`) with neither the refund pill nor a path to Final Review. Separately, the header pill only ever shows **Federal** refund (no state-refund pill anywhere in the header). Consequence: the prompt's step 3 ("review the state refund in the header pill … click Continue to Final Review") is impossible for the seeded completed CA return. Steps 1–2 (state adjustments + CASDI deduction) do work. **Fix deferred** — needs product intent: the completed-state branch dead-ending to "Fix Return" may be intentional demo content, and a state-refund header pill would be new UI. Flagged, not implemented (same disposition class as medium-011/013).

## Fix

_Link PR/commit once pushed; set status fix-pushed → verified._

- PR: https://github.com/turing-rlgym/returnmax/pull/271 (→ `ots_dev`). **Fixed 2026-06-15.** Two commits:
  - `app/index/tto/state/review/_client.tsx` — inverted the gate: the "Fix Return" dead-end now shows only for a **non-completed** state; a `completed` state (seed's CA) falls through to the existing refund + **Continue to Final Review** success view. (`completed` = done everywhere else, e.g. the green badge in `status/_client.tsx`.)
  - `components/app/AppHeader.tsx` — header pill reads the **state** refund while on `/index/tto/state/*` (`CA state refund $815.50`, 2-decimal since state refunds carry cents); federal everywhere else; "Explain my taxes" panel stays federal.
  - Verified live (chrome-cdp, turing profile): pill shows `CA state refund $815.50` on `/state/review`, reverts to `Federal refund` on `/final-review`; success view + Continue button present. Display/nav only → no state diff. `pnpm typecheck` + eslint clean.

