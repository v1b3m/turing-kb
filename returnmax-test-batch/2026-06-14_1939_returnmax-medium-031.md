---
task_id: "returnmax-medium-031"
version: "v1"
prompt_uuid: "eed1723e-109b-43ee-93b8-f402405573a2"
task_url: "https://staging-rl-gym-harness.turing.com/verification/prompts/eed1723e-109b-43ee-93b8-f402405573a2/task"
batch: "ReturnMax Test Batch"
# --- coordination (source of truth for multi-agent runs) ---
status: fix-pushed        # untested | in-progress | passed | bug-found | fix-pushed | verified
owner: claude-runner                  # agent id holding the claim; empty = available
claimed_at: 2026-06-15 14:05             # ISO-8601 UTC+3 when claimed (stale lease if old)
updated_at: 2026-06-15 14:32             # ISO-8601 UTC+3 of last status change
# --- results ---
bug_count: 2
fix_pr: https://github.com/turing-rlgym/returnmax/pull/283
# --- prompt metadata (immutable, latest version) ---
l1: "Account & Settings"
l2: "Account Management"
l3: "Manage Account Settings, Order Summary, and Download Tax Return"
persona: "Returning User"
complexity: "Medium"
ecosystem: "Tax"
est_hops: "3"
est_steps: "44"
est_actions: "4"
gym: "Returnmax V1"
gym_url: "https://returnmax-v1.rlgym.turing.com"
tags: [returnmax-test, rlgym]
---

# returnmax-medium-031 (v1)

Batch index: [[2026-06-14_1939_returnmax-test-batch-prompts]] · [Open task page](https://staging-rl-gym-harness.turing.com/verification/prompts/eed1723e-109b-43ee-93b8-f402405573a2/task) · Gym: [Returnmax V1](https://returnmax-v1.rlgym.turing.com)

> [!warning] Before testing: claim + reset to clean state
> 1. Claim: set `owner`, `status: in-progress`, `claimed_at` (only if `owner` empty/stale). See [[_AGENT-PROTOCOL]].
> 2. Clean state: open `https://returnmax-v1.rlgym.turing.com/state-diff` → **Reset All** → confirm all sections **Clean**.
> 3. Drive the app through the prompt, then re-open **state-diff** to verify the change (oracle).

- Taxonomy: Account & Settings › Account Management › Manage Account Settings, Order Summary, and Download Tax Return
- Persona: Returning User | Complexity: Medium | Est. hops/steps/actions: 3/44/4

## Prompt

> Need to pull everything together for records after the 2024 return was filed. From the My Account section, open the Download Tax Return page (/index/tto/download) and download the federal PDF and the worksheets PDF using their individual download buttons, note the IRS retention notice shown. Then navigate to the Receipt page (/index/tto/receipt) and confirm the 2024 order receipt is in 'paid' state showing the correct line items: Federal return preparation ($89.00), California state return preparation ($59.00), and Audit support ($39.00) with a total of $187.00 and payment method 'Visa **** 4242'. Finally open the Clear & Start Over page and read the data-deletion warning list without confirming, then navigate away without clicking Delete.

## Prep work

> User Maya Srinivasan (user_id: aecd2ba0) has a filed 2024 return. Download Tax Return page (/index/tto/download) offers 5 download formats: .tax file, federal PDF, state PDF, worksheets PDF, and summary PDF, each with a download button and an IRS retention notice. Receipt page (/index/tto/receipt) shows order receipt (order_receipt_id: ee960861) in 'paid' state with line items: 'Federal return preparation' $89.00, 'California state return preparation' $59.00, 'Audit support' $39.00; total $187.00; payment method 'Visa **** 4242'; paid at 2025-03-17. Clear & Start Over page (/index/tto/clear-start-over) presents a red-styled warning with a data-deletion list and checkbox confirmation before the Delete button.

## Justification

A returning filer downloads two specific return documents for record-keeping, reviews and confirms the accuracy of a prior paid receipt including all line items and payment method, then examines the Clear & Start Over warning page to understand what data would be lost without committing to the action.

## Findings

**Run 2026-06-15 14:05–14:25 (claude-runner) — `localhost:3001`, baseline `ots_dev` @ `42a0d1b`. Clean seed verified (`rm_tax_return.w2s[0].wages===92500.75`, Luma W-2 present).**

Three-step records task. Step 1 (Download page) is broken; Step 2 (Receipt) data is correct but the 2024 receipt is unreachable through the UI; Step 3 (Clear & Start Over) works.

1. **Step 1 — Download Tax Return (`/index/tto/download`).** ✗ **404 — the page does not exist.** No 5-format download UI, no individual download buttons, no IRS retention notice. **Root cause (git):** the page was a real, built feature (`app/index/tto/download/{page,_client}.tsx`) **deleted in commit `63abd2b` "Remove unreachable download and estimated-vouchers pages"** (2026-06-13). The deleted `_client.tsx` is an *exact* match for the prompt: 5 formats (`.tax`, Federal Return PDF, State Return PDF, Supporting Worksheets PDF, Tax Summary PDF) each with a Download button, a "Download all" button, and the IRS retention notice ("Keep copies of your tax return for at least 3 years. The IRS can generally audit returns filed within the last 3 years, or 6 years if income was substantially understated."). Same disposition class as **medium-018** (page deleted as a stopgap, prompt expects it) → **restorable surgical fix**, not a from-scratch build.
2. **Step 2 — Receipt (`/index/tto/receipt`).** ◐ The **2024 receipt data is fully correct** but is **unreachable via the UI**. The page reads `receiptsByYear[taxYear]` defaulting to `CURRENT_TAX_YEAR=2025` (which has no receipt → renders "Your receipt is not available"). The seed's `rm_order_receipt` has the **2024** paid receipt, reachable only via `?receipttaxyear=2024` — rendering it there shows exactly: Federal return preparation **$89.00**, California state return preparation **$59.00**, Audit support **$39.00**, Total **$187.00**, "Paid with Visa **** 4242", "March 17, 2025" ✅. But **both** receipt nav links in the app hardcode `receipttaxyear=2025` (`app/unified/[year]/index/_client.tsx:194`, `components/index/tto/TaxHomeResourceAccordions.tsx:141`), and the "ReturnMax account" / My Account menu is a `showComingSoon` stub (`UnifiedSidebar.tsx:487,808`). So "From the My Account section … navigate to the Receipt page … confirm the 2024 order receipt" cannot be done through the UI — the bare/linked receipt page shows the empty 2025 receipt. Data correct; surfacing of the 2024 receipt is the gap.
3. **Step 3 — Clear & Start Over (`/index/tto/clear-start-over`).** ✅ works exactly as described. Red "This action cannot be undone" warning + data-deletion list (Personal information and filing status / All income entries (W-2s, 1099s, etc.) / Deductions and credits / State return information / Uploaded documents), a checkbox confirmation ("I understand that all my tax return data will be permanently deleted…"), and a **disabled** "Delete and start over" button (enabled only after checking) plus Cancel. Reading the list and navigating away without confirming is fully achievable. Linked from the sidebar (`UnifiedSidebar.tsx:194,793`).

State-diff oracle: only reads/navigation performed (no Delete, no receipt download); no state mutation. Clean seed unchanged.

**Verdict: bug-found.** Primary fixable bug: `/index/tto/download` 404s because the matching page was deleted as "unreachable" (commit `63abd2b`) — restorable, mirroring medium-018's #281. Secondary: the 2024 receipt is correct but unreachable via UI (links hardcode year 2025; My Account is a coming-soon stub). Step 3 works.

## Bugs found

_One bullet per bug; keep `bug_count` in sync._

- **`/index/tto/download` (Download Tax Return) 404s — the page was deleted in commit `63abd2b` ("Remove unreachable download and estimated-vouchers pages") though the prompt expects it.** The deleted `app/index/tto/download/{page,_client}.tsx` exactly matches the prompt's Step 1 (5 download formats with individual buttons + IRS retention notice). Consequence: Step 1 (download federal PDF + worksheets PDF, note IRS notice) is impossible. **Fix: restore the page** (surgical revert of the deletion), same disposition as medium-018/#281. (PR below.)
- **The 2024 order receipt is correct but unreachable through the UI.** `/index/tto/receipt` defaults to year 2025 (empty); the 2024 paid receipt (`rm_order_receipt`, all line items/total/payment correct) is reachable only via `?receipttaxyear=2024`, and every in-app receipt link hardcodes `receipttaxyear=2025` (`unified/[year]/index/_client.tsx:194`, `TaxHomeResourceAccordions.tsx:141`); the "ReturnMax account" menu is a `showComingSoon` stub. So the prompt's "from My Account → Receipt → confirm 2024 receipt" can't be completed via the UI. Data is correct; **surfacing** is the gap. (Flagged; no surgical single-fix without product direction on which year the receipt should default to.)

## Fix

_Link PR/commit once pushed; set status fix-pushed → verified._

- **PR https://github.com/turing-rlgym/returnmax/pull/283** (branch `fix/medium-031-download-page`). Restores `app/index/tto/download/{page,_client}.tsx` verbatim from `63abd2b^` (the commit that deleted them). Only the download page is restored — not estimated-vouchers. `pnpm typecheck` clean; pre-commit `eslint --fix` normalized the files. **Live-verified on the working-tree branch** (chrome-cdp `turing`, localhost:3001, clean seed): `/index/tto/download` now **200s**, h1 "Download your tax return", all **5 formats** (.tax, Federal Return, State Return, Supporting Worksheets, Tax Summary) with download buttons, "Download all" button, and the **IRS retention notice** ("Keep copies of your tax return for at least 3 years… 6 years if income was substantially understated."). Step 1 is now performable.
- **Receipt (bug #2) not fixed here** — flagged only. The 2024 receipt data is correct (renders via `?receipttaxyear=2024`); surfacing it through the UI (links hardcode 2025, My Account is a coming-soon stub) needs product direction on default-year behavior. Out of scope for this surgical restore.
- Status: **fix-pushed** (PR #283 pending merge). Will move to `verified` post-merge on a clean re-test, per the medium-018 pattern.

