---
task_id: returnmax-medium-010
version: v1
prompt_uuid: dcfca7d3-f92d-4f37-9df4-3b51b5bb9e75
task_url: https://staging-rl-gym-harness.turing.com/verification/prompts/dcfca7d3-f92d-4f37-9df4-3b51b5bb9e75/task
batch: ReturnMax Test Batch
status: verified
owner: claude-runner
claimed_at: 2026-06-15 10:09
updated_at: 2026-06-15 11:09
bug_count: 1
fix_pr: local commits f6e36a4 + 7b82473 on branch chore/more-fixes-2 (not yet pushed/PR'd)
l1: Deductions & Credits
l2: Tax Credits
l3: Claim Child Tax Credit, EITC, Child Care, and Education Credits
persona: First-Time Filer
complexity: Medium
ecosystem: Tax
est_hops: "4"
est_steps: "48"
est_actions: "4"
gym: Returnmax V1
gym_url: https://returnmax-v1.rlgym.turing.com
tags:
  - returnmax-test
  - rlgym
---

# returnmax-medium-010 (v1)

Batch index: [[2026-06-14_1939_returnmax-test-batch-prompts]] · [Open task page](https://staging-rl-gym-harness.turing.com/verification/prompts/dcfca7d3-f92d-4f37-9df4-3b51b5bb9e75/task) · Gym: [Returnmax V1](https://returnmax-v1.rlgym.turing.com)

> [!warning] Before testing: claim + reset to clean state
> 1. Claim: set `owner`, `status: in-progress`, `claimed_at` (only if `owner` empty/stale). See [[_AGENT-PROTOCOL]].
> 2. Clean state: open `https://returnmax-v1.rlgym.turing.com/state-diff` → **Reset All** → confirm all sections **Clean**.
> 3. Drive the app through the prompt, then re-open **state-diff** to verify the change (oracle).

- Taxonomy: Deductions & Credits › Tax Credits › Claim Child Tax Credit, EITC, Child Care, and Education Credits
- Persona: First-Time Filer | Complexity: Medium | Est. hops/steps/actions: 4/48/4

## Prompt

> Credits need to be finalized for Ella before the return goes to review. On the Child Tax Credit page, confirm Ella Rivera (DOB: 2017-09-12) is listed as a qualifying child and that the $2,000 credit amount is assigned to her. Then on the Child and Dependent Care Credit page (Form 2441), add the childcare provider 'Mission Kids After School Club' with TIN '81-2345678' and enter the amount paid of $3,600.00. Finally navigate to the Earned Income Credit wizard and complete the eligibility flow for a Head of Household filer with one qualifying child, confirming the EIC estimate populates in the header.

## Prep work

> Tax return for Maya Srinivasan (tax_return_id: 6aafcfc5) is in progress; filing status 'head_of_household'. Dependent 'Ella Rivera' (dependent_id: b65b7cd2) exists as a qualifying child with DOB 2017-09-12. Child Tax Credit page exists under Deductions & Credits; $2,000 credit is valid for one qualifying child. Child & Dependent Care Credit page (Form 2441) exists; valid provider name is 'Mission Kids After School Club', provider TIN '81-2345678', amount paid $3,600.00. Earned Income Credit wizard exists under Deductions & Credits with eligibility gating based on income, filing status, and qualifying children. The header refund pills update after each credit is saved.

## Justification

A head-of-household filer with one qualifying child confirms the Child Tax Credit assignment, adds a childcare provider and expense amount for the Dependent Care Credit, and completes the EIC eligibility flow to ensure all three dependent-linked credits are captured before final review.

## Findings

**Run 2026-06-15 10:09–10:14 (claude-runner) — `localhost:3001`, baseline `ots_dev`. Latest version re-fetched from harness API = v1 (matches note).**

Three credit parts:

**Part 1 — Child Tax Credit: ✅ works.** `/deductions/child-tax-credit` is a gate ("Do you want us to check this credit?"). Selecting **Yes → Continue** advances to the result that lists **Ella Rivera** as a qualifying child with the **$2,000** credit assigned. Verified on screen. Screenshot `assets/medium-010-ctc.png`.

**Part 2 — Child & Dependent Care Credit (Form 2441): data correct, credit computed (stale seed).** The seed already contains the provider the prompt asks to "add": `credits.childDependentCare.providers[0]` = **Mission Kids After School Club**, TIN **81-2345678**, amountPaid **3600** (plus address). The engine computes the credit (`getChildDependentCareCreditTotal`, `lib/tax-engine.ts:301`) and it flows into total credits. So nothing to add — same stale-seed pattern as 001/002/004/007. (The multi-step `/deductions/child-care` wizard — gate → "uncommon situations" Yes/No → provider/amount → review — is navigable; I verified the underlying seeded data is correct rather than re-typing it.)

**Part 3 — Earned Income Credit: prompt unachievable; app shows a hardcoded "not qualified".** `/deductions/earned-income-credit` immediately renders **"It looks like you don't qualify"** — there is no eligibility wizard and no EIC-estimate path. Two distinct problems:

1. **Prompt is factually wrong for this filer.** Maya is HoH with AGI ≈ $90,240. The 2025 EIC income limit for HoH/1 child is ≈ $49,084, so she is **correctly not EIC-eligible**. The prompt's "complete the eligibility flow … confirming the EIC estimate populates in the header" cannot happen — a $90k filer has **$0 EIC** by law. So even a fully-correct app would not populate an EIC estimate here.
2. **App EIC flow is a not-qualified-only stub (latent bug).** `EarnedIncomeCreditFlow` (`app/index/tto/deductions/earned-income-credit/earned-income-credit-flow.tsx:13-18`) unconditionally renders `EarnedIncomeCreditNotQualifiedStep`, and the view type is literally `EarnedIncomeCreditView = "not-qualified"` (only one value; `lib/earned-income-credit-flow.ts:1`). There is **no income/filing-status/qualifying-children gating** despite prep_work claiming it. For Maya the "not qualified" output is *coincidentally correct*, but **any genuinely EIC-eligible filer (low income + qualifying child) would be wrongly told they don't qualify** — EIC can never be claimed in this app. See Bugs found.

**Verdict: bug-found.** Parts 1–2 work (CTC confirms Ella + $2,000; child-care provider data correct and credit computed). Part 3 cannot be completed: the app's EIC flow is a hardcoded "not-qualified" stub with no eligibility logic or estimate path, and separately the prompt's EIC expectation is factually impossible for a ~$90k filer. State restored to clean seed afterward.


## Bugs found

_One bullet per bug; keep `bug_count` in sync._

- **EIC eligibility is never evaluated — the Earned Income Credit flow always renders "It looks like you don't qualify".** `app/index/tto/deductions/earned-income-credit/earned-income-credit-flow.tsx:13-18` unconditionally renders `EarnedIncomeCreditNotQualifiedStep`; the flow view type is `"not-qualified"`-only (`lib/earned-income-credit-flow.ts:1`); `estimateEarnedIncomeCredit` returns 0 whenever `credits.earnedIncomeCredit` is null/not-applicable (`lib/tax-engine.ts:254`), and nothing in the flow ever sets it to applicable. Consequence: **no filer can ever qualify for or claim EIC**, and the prompt's "EIC estimate populates in the header" is impossible. Not triggered as a *wrong dollar amount* for Maya (she is genuinely ineligible at ~$90k AGI), but it is a real correctness gap for any eligible filer. **✅ FIXED 2026-06-15** — real 2025 EIC eligibility + estimate implemented (see Fix).

## Fix

_Link PR/commit once pushed; set status fix-pushed → verified._

**Fixed 2026-06-15 (claude-runner) — commits `f6e36a4` + `7b82473` on branch `chore/more-fixes-2` (local, not yet pushed/PR'd).**

Implemented genuine 2025 EITC eligibility + estimate so eligible filers qualify and ineligible ones (Maya) are turned away by *logic*, not a hardcode:

- **`lib/tax-constants.ts`** — `EIC_PARAMS_2025` (phase-in/phaseout rates, max credit, phaseout-begin thresholds by filing status, indexed by 0/1/2/3+ qualifying children) and `EIC_INVESTMENT_INCOME_LIMIT_2025 = 11950`. Verified against IRS EITC tables / Rev. Proc. 2024-40 (e.g. HoH/1-child max $4,328, completed phaseout $50,434).
- **`lib/earned-income-credit.ts`** — `computeEarnedIncomeCredit(state, agi)` runs the standard worksheet (phase the credit in at the bracket rate up to the max, phase out by the greater of AGI/earned income above the threshold, $0 if investment income exceeds the cap), plus `computeInvestmentIncome` and `countEicQualifyingChildren` (own child <19, <24 if student, any age if disabled).
- **`lib/tax-engine.ts`** — `estimateEarnedIncomeCredit` now delegates to the shared worksheet (still gated on the filer engaging the flow) so the header refund pill matches the stored amount.
- **EIC flow** (`lib/earned-income-credit-flow.ts` + `earned-income-credit/*`) — replaced the stub with an intro gate → qualified result (shows the estimate) / not-qualified screen, mirroring the Child Tax Credit pattern. Also stopped writing `notApplicable` on page load (visiting the page no longer mutates state).

**Live-verified (localhost:3000, chrome-cdp `turing` profile):**
- Maya (HoH, ~$92.5k W-2, Ella 1 child): intro → Yes → **"It looks like you don't qualify"** — now logic-driven (genuinely ineligible). Visiting the page left `credits.earnedIncomeCredit` = `null` (no init-write noise).
- Eligible scenario (wage temporarily set to plateau $12,730, then restored): intro → Yes → **"You qualify for the Earned Income Credit"** with estimate **$4,328**; Continue persisted `{ amount: 4328 }`; header refund pill updated.
- `pnpm typecheck` + `eslint` on touched files clean. State restored to original afterward.

**Prompt caveat unchanged:** medium-010 still cannot be "passed" as written — Maya at ~$90k AGI has $0 EIC by law, so "the EIC estimate populates in the header" is factually impossible for this filer. The fix addresses the latent app bug (no filer could ever claim EIC), not the impossible prompt expectation.

