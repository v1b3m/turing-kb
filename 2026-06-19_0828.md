![[Pasted image 20260619082838.png]] 

I need to know why the "Foreign Accounts" and "Medical Expenses" review items appear to just have been added however, the third which I assume is "Personal Info" is just modified from "needs_fix" to "fixed" implying it already existed. At what point are they added? Should they be seeded into default state JSON? Or should they be added when the user reviews?

You can also view from the screenshot that `totalIssuesFound` changes from a Seed Value of 1 to a Current Value of 2, this is misleading as the review items are initially 3 and one gets fixed so I'd expect 3 -> 2

From the third item, I see `digitalAssetAnswer` change from `null` to `yes`. Is this the review item that gets fixed. I think I was wrong to assume Personal Info.
