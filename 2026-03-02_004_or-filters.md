**Ref:** main
**Depends:** 2026-03-01_001_more-ui-updates

### Context

![[Pasted image 20260302004127.png]]

Hitting 'OR' on the other end, replicates the same filter on a row right below the filter row whose **OR** button has been clicked. Check the screenshot above for reference.

If **OR** is hit more than once, the next row should be added below the last OR in the block, multiple **OR**s can be added

The Filters have Close (X) buttons which can remove the filter entirely

The pin icon should be placed in a button just like the other buttons in its row.

Hitting the pin button should fill the button's bg with the same color as the dark header and make the pin icon white outline

---

Clicking the **OR** button in the header row behaves a little different

![[Pasted image 20260302005353.png]]

A new section is added separated by a divider

---

Clicking **AND** button from the header row adds a simple row, no dividers here

![[Pasted image 20260302005725.png]]
### Acceptance Criteria
- [ ] OR filter behavior matches spec
- [ ] Clicking the **OR** button in the header row adds an OR section at the bottom of the filter section
- [ ] Clicking the **AND** button in the header row adds an AND section at the bottom of the filter section
### Constraints
- None

### Resources
- None

---

### Handoff

| Field | Details |
|---|---|
| **What was done** | Implemented full OR filter functionality: row-level OR (duplicates parent row's field/operator as an "or" sub-row), header OR (adds new section with divider and "OR all of these conditions must be met" heading), header AND (adds simple row to last section). Pin button now toggles between outlined and dark-bg filled state. Store updated with FilterSection/FilterGroup/FilterCondition types. Table filtering logic updated to support OR between sections, AND within sections, OR within groups. |
| **Commit hash(es)** | `9f99f4a` |
| **Files touched** | `src/stores/useKnowledgeStore.ts`, `src/components/knowledge/FilterPanel.tsx`, `src/components/knowledge/KnowledgeArticlesTable.tsx` |
| **Decisions made** | Data model: sections (OR'd) → groups (AND'd) → conditions (OR'd). This three-level nesting maps directly to the UI: header OR creates sections, rows within a section are AND'd, row-level OR creates conditions within a group. Kept `andFilters` for backward compatibility with breadcrumb display. Pin state is local to FilterPanel (not persisted to store). |
| **Known limitations** | Static hardcoded rows (Workflow, Author, Revised By) don't participate in the dynamic filter system — their OR buttons are no-ops. Pin toggle is visual only (doesn't persist panel visibility). |
| **How to verify** | `npm run build` succeeds. Open filter panel → click header AND → new empty row appears. Click header OR → new section with divider appears. Fill a row with field/value, click row OR → "or" sub-row appears below with same field/operator. Click Run → filters apply with correct OR/AND logic. Pin button toggles dark/light. |
