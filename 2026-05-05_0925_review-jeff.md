Check what the branch `goffrey/QAfixes` is trying to bring into `ots_dev`.

Below is the PR description:

```
## Summary
- Implemented a full Picture-in-Picture (PiP) flow separate from the mini-player with a dedicated store and global renderer.
- Added hover-based PiP control (middle-right) and a PiP placeholder state in the watch player.
- Enabled seamless video switching while PiP is active when navigating to a new `/watch` URL.
- Prevented mini-player and PiP conflicts and resolved state handoff race conditions.
- Fixed playback continuity issues by ensuring restore from PiP or mini-player keeps playback running via `autoplay=1`.
- Prevented watch page player mount from overriding active PiP playback state.
- Updated playlists UX so `/feed/playlists` only shows private playlists owned by the active account.
- Resolved React snapshot warning by stabilizing viewer-store selection logic.
- Improved Save-to-playlist flow:
  - Updated CTA text from **Change** → **See list**
  - Converted CTA into a real link to `/feed/playlists`
  - Increased toast duration for better usability
- Updated sidebar and creator channel behavior:
  - “Your videos” now shows **Coming soon**
  - “Customize channel” shows **Coming soon**
  - Added **Community** action pill linking to a dedicated page
- Implemented new community routing:
  - Added `/@handle/community` route with rewrites/proxy support
  - Added owner/community pages and connected creator navigation
- Built new community UI:
  - Matches provided mock (large header + updated composer)
  - Preserved legacy posts tab UI on `/@johndoe`
  - Community uses new layout, Posts retains original design
- Removed channel header on community page using `showHeader` toggle
  - Community route renders with `showHeader={false}`


## Test plan
- [ ] Hover player and verify PiP icon appears at middle-right.
- [ ] Activate PiP and verify watch player shows placeholder and floating PiP appears.
- [ ] While PiP is active, open another `/watch?v=...` and verify video switches and continues playing.
- [ ] Restore from PiP and confirm playback continues (not paused).
- [ ] Restore from mini-player and confirm playback continues.
- [ ] Verify mini-player does not activate while PiP is active.
- [ ] Save to playlist and confirm CTA displays **See list**.
- [ ] Click **See list** and verify navigation to `/feed/playlists`.
- [ ] Confirm toast remains visible long enough (~8 seconds).
- [ ] On `/feed/playlists`, verify only private playlists owned by the active account are shown.
- [ ] Click “Your videos” in sidebar and verify **Coming soon** modal appears.
- [ ] In creator view, click “Customize channel” and verify **Coming soon** modal.
- [ ] Click Community action pill and verify navigation to `/@johndoe/community`.
- [ ] On `/@johndoe/community`, verify channel header is hidden.
- [ ] Confirm community page layout matches expected UI (header block, status pills, composer).
- [ ] On `/@johndoe` → Posts tab, verify legacy posts UI remains unchanged.
  
```





## Review handoff - 2026-05-05 09:35

Compared `origin/goffrey/QAfixes` against `origin/ots_dev`. No source changes or commits were made.

Findings:

1. `src/components/video/SaveToPlaylistDialog.tsx:77` / `:102` - `useEffect` is declared after an early return, so opening the dialog after an initial hidden render changes hook order and can trigger a React runtime hook error. Move the cleanup effect above the early return.
2. `src/components/video/VideoPlayer.tsx:135` and `src/components/video/MiniPlayer.tsx:226` - activating PiP keeps the mini-player store populated with the playing watch session. If the user navigates away while PiP is active and then closes PiP, `GlobalMiniPlayer` can auto-open the stale mini-player, which reintroduces the PiP/mini-player conflict. Clear or pause the mini-player session when entering/closing PiP.
3. `src/components/video/GlobalPictureInPicture.tsx:184` - unused `GlobalPictureInPictureContent` should fail the configured unused-vars lint rule and should be removed.

Verification:

- Reviewed the full branch diff and line-numbered files for PiP/player, playlist, save-to-playlist, sidebar, and community routing changes.
- `git diff --check origin/ots_dev...HEAD` passed in an isolated review worktree.
- `npm run typecheck` was attempted in an isolated review worktree but was blocked because the available dependency install is missing `@radix-ui/react-popover` despite it being declared in `package.json`.
- Focused ESLint on the touched video files was started but did not finish promptly with the current dependency/config setup; stopped it and recorded the static lint issue from config inspection.

Status: needs fixes before merge. Suggested column: handoff/review, not Completed.
